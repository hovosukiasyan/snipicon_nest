import { DesignerCollectionEntity } from './designer-collection.entity';

describe('DesignerCollectionEntity', () => {
  it('should be defined', () => {
    expect(new DesignerCollectionEntity()).toBeDefined();
  });
});
