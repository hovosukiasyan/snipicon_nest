import { NormalisedIconEntity } from './normalised-icon.entity';

describe('NormalisedIconEntity', () => {
  it('should be defined', () => {
    expect(new NormalisedIconEntity()).toBeDefined();
  });
});
